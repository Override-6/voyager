<stall-review>
The last {{rounds}} rounds made no measured progress (progress probe: {{trend}}; first Now item: "{{now}}"). More of the same will not fix this. Before anything else, do this review, write its result in PLAN.md (Current phase + one Log line), then continue:
1. What did the last rounds actually try, and what came out of each attempt? Write attempts and observed results apart from assumptions.
2. Is the first Now item too big? Split it until the next step is one experiment you can run and verify in at most 3 tool calls (a `repl` call against the live system is ideal).
3. Are you verifying by thinking or reading instead of by running? Anything you can try in one call, try now.
4. If the route itself is wrong, go back to knowledge/approach.md, pick another approach, and record why.
</stall-review>
