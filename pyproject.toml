[project]
name = "voyager"
version = "0.1.0"
description = "The Voyager pattern for local LLMs: long-running missions that survive any number of context compactions, with memory, plans and tools kept in a workspace"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=1.0",
    "httpx2>=2.13.0",
    "lxml>=6.1.3",
    "mcp>=2.2.0",
    "prompt_toolkit>=3.0.43",
    "trafilatura>=2.2.0",
]

[project.scripts]
voyager = "voyager.cli:main"

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/voyager"]
